#import * from './generated/prisma.graphql'

type Mutation {
    createWoman(name: String, description: String): Woman
}

type Query {
    women: [Woman]!
}

mutation createAWoman {
  createWoman(name:"Waverly Mandel", description:"Waverly has done so many cool things") {
    name
    description
  }
}